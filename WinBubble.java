import greenfoot.*;

/**
 * Write a description of class WinBubble here.
 * 
 * @author (your name) 
 * @version (a version number or a date)
 */
public class WinBubble extends ScrollingActor
{
    /**
     * Act - do whatever the WinBubble wants to do. This method is called whenever
     * the 'Act' or 'Run' button gets pressed in the environment.
     */
    public void act() 
    {
        /*
        if (Greenfoot.isKeyDown("enter")) {
            Greenfoot.setWorld(new GameWorld());
            Greenfoot.start();
        }
        */
    }    
}
